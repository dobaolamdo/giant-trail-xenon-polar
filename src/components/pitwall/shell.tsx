import { useEffect, useMemo, useState, type ReactNode } from "react";
// RESTORED - full file follows in next commit
export function PitwallShell() {
  return <div className="p-8 text-fg">Deploying fix… refresh in 1 min</div>;
}
